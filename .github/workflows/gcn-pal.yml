name: GCN PAL Tests

on:
  pull_request:
    branches: [ master ]

jobs:
  build:
    runs-on: ubuntu-latest
    container:
      image: ghcr.io/pheenoh/zsrtp-tpgz:gcn_pal
      options: --shm-size=128m --ulimit core=-1 --privileged=true
    steps:
    - name: Checkout
      uses: actions/checkout@v4
      with:
        token: ${{ github.token }}
        submodules: recursive
    - name: Move ISO
      run: mkdir -p ./isos/ && mv /GZ2P01.iso ./isos/GZ2P01.iso
    - name: Compiler Test
      run: mkdir build && cd build && cmake .. -DPR_TEST=1 -DPLATFORM=GCN -DREGION=PAL && make -j$(nproc) iso && cd ..
    - name: Boot Dolphin
      run: |
        dolphin-emu-nogui --platform=headless --video_backend=Null -e build/tpgz.iso &
        sleep 15
        /dme -c 0x80452540